<section>
  {{#each this}}
  <div class="container ">
    <div class="row justify-content-md-center">
      <div class="card  p-3 mb-1   " style=" margin-top:5px ;">
        <div>
          <h5> {{user.username}}</h5> on <span>{{format_date created_at}}</span>
        </div>
        <hr>
        <div class="card-body ">
          <h5> {{comment_text}}</a></h5>
        </div>
      </div>
    </div>
  </div>
  {{/each}}
</section>